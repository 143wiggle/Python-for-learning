# 1.) A simple function that prints a greeting. No inputs required.
# Defines the function using 'def' and gives it a name
def say_hello():
    # Block of code that runs only when the function is called
    print("Hello there!")

# Calls the function (executes it)
say_hello()
