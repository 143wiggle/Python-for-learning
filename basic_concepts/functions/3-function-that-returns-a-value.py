# 3.)Returns the result instead of printing it directly.
# Function takes two inputs and returns their sum
def add(a, b):
    return a + b  # Return sends data back to where the function was called

# Stores the returned result in a variable
result = add(3, 5)

# Outputs the result
print(result)
