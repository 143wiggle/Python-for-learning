# Defines a function that takes a parameter called 'name'
def greet_person(name):
    # Uses the input to customize the greeting
    print("Hi " + name + "!")

# Calls the function and passes an argument to the parameter
greet_person("Wigor")
